# AGENTS.md

이 문서는 이 저장소에서 기능 개선, 기능 추가, 리팩토링을 진행하는 에이전트와 협업자를 위한 작업 규칙입니다. 필요할 때마다 실제 코드베이스 운영 방식에 맞춰 갱신합니다.

## 기본 원칙

- Next.js App Router, TypeScript, React 기반 프로젝트입니다.
- `app/`은 라우팅과 레이아웃만 담당하고, 실제 화면/비즈니스 로직은 `features/` 아래 도메인 단위로 둡니다.
- 여러 영역에서 재사용되는 레이아웃, UI, 유틸은 기존 위치와 패턴을 먼저 확인한 뒤 `components/` 또는 `lib/`로 분리합니다.
- 기존 사용자 변경사항을 되돌리지 않습니다. 작업 범위와 무관한 파일은 수정하지 않습니다.

## 주석 규칙

- 새로 작성하거나 의미를 변경하는 함수, 컴포넌트, 훅, 타입, 상수, 주요 변수에는 한글 JSDoc 주석을 작성합니다.
- JSDoc은 "무엇을 하는지", "왜 필요한지", "파라미터/반환값의 도메인 의미"를 간결하게 설명합니다.
- 구현을 그대로 반복하는 설명은 피하고, 호출자가 알아야 하는 제약이나 부작용이 있으면 주석에 포함합니다.
- 기존 코드에 주석이 없는 경우도, 해당 식별자를 수정한다면 한글 JSDoc을 함께 보강합니다.

```ts
/**
 * 지원자 목록 화면에서 사용할 파트 쿼리 값을 검증해 표준 필터 값으로 변환합니다.
 *
 * @param value URLSearchParams에서 읽은 원본 파트 값
 * @returns 화면에서 허용하는 파트 필터 값
 */
function parsePartParam(value: string | null) {
  // ...
}
```

## TypeScript와 데이터 처리

- `api-docs.json`은 백엔드 API 명세서의 로컬 사본입니다. API 연동, 요청/응답 타입, 엔드포인트, 파라미터, 인증 흐름을 수정할 때는 먼저 이 파일의 관련 항목을 확인합니다.
- 코드와 `api-docs.json`이 다르면 임의로 추측하지 말고 `api-docs.json`을 기준으로 구현하되, 명세가 모호하거나 실제 동작과 충돌하는 부분은 작업 결과에 명시합니다.
- API 응답과 요청 payload는 `features/**/type.ts` 또는 도메인별 `types.ts`에 명확히 타입을 둡니다.
- `unknown`이나 넓은 `string` union은 API 명세가 불완전할 때만 사용하고, 화면 진입부에서 가능한 한 정규화합니다.
- 날짜/시간은 사용자 화면 기준으로 `Asia/Seoul`을 명시해 포맷팅합니다.
- URL 쿼리, route param, enum-like 문자열은 별도 파서/정규화 함수로 검증한 뒤 사용합니다.

## Admin 작업 규칙

- `/app/admin/**` 라우트 파일은 얇게 유지하고, 실제 구현은 `features/admin/**`에 둡니다.
- 지원자 관리, 면접 관리, 아기사자 관리, 운영진 주요업무는 서로 다른 도메인으로 보고 공통화는 중복이 충분히 확인된 뒤 진행합니다.
- 권한/역할 판단은 `features/admin/permissions.ts` 같은 단일 경계로 모으고, 페이지마다 조건을 새로 만들지 않습니다.
- 합격 처리, 삭제, 복구, 출석 저장처럼 되돌리기 어려운 액션은 비활성화 조건, 로딩 상태, 실패 메시지를 함께 다룹니다.
- 리스트 필터/페이지네이션 상태는 URL 쿼리와 화면 state 중 하나를 기준으로 삼고, 둘을 동시에 갱신할 때는 동기화 책임을 명확히 분리합니다.

## 검증

- 코드 변경 후 가능한 경우 `npm run lint`를 실행합니다.
- 배포 관련 변경을 건드렸다면 `npm.cmd run build`와 `npm run package:standalone`을 확인합니다.
- admin 화면 변경은 최소한 목록, 상세, 필터, 페이지네이션, 권한 없는 상태, API 실패 상태를 함께 점검합니다.
